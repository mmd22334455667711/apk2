# راهنمای اجرا (TLS Test App)

## چرا نمی‌تونم APK آماده بدم؟
ساخت این محیط (سندباکس) به سرورهای SDK اندروید دسترسی نداره، پس نمی‌تونم خودم
کامپایلش کنم. ولی کل سورس‌کد آماده‌ست، فقط باید توی Android Studio بازش کنی.

## مراحل

### ۱. باز کردن پروژه
- Android Studio رو باز کن.
- Open an existing project → این پوشه (TlsTestApp) رو انتخاب کن.
- بذار Gradle Sync خودش انجام بشه (ممکنه چند دقیقه طول بکشه، اینترنت لازم داره
  چون باید کتابخونه‌های OkHttp و Conscrypt رو دانلود کنه).

### ۲. تنظیم آدرس تست
فایل زیر رو باز کن:
```
app/src/main/java/com/example/tlstest/MainActivity.java
```
خط زیر رو پیدا کن:
```java
private static final String TEST_URL = "https://example.com/";
```
و آدرسش رو با آدرس واقعی سرور تستت عوض کن. دو گزینه داری:

**گزینه A: تست ساده با یه سایت عمومی (سریع‌ترین راه)**
```java
private static final String TEST_URL = "https://www.google.com/";
```
این بهت می‌گه گوشی اصلاً توانایی TLS مدرن رو (با کمک Conscrypt) داره یا نه،
بدون این‌که نیاز به راه‌اندازی سرور خودت داشته باشی.

**گزینه B: تست با تانل خودت (ngrok یا cloudflared)**
اگه سرور تست رو طبق راهنمای قبلی بالا آوردی، همون آدرس https رو بذار.

### ۳. نصب روی گوشی
- گوشی رو با USB وصل کن (USB Debugging که گفتی فعاله، پس مشکلی نباید باشه).
- دکمه‌ی Run (▶) رو بزن، دستگاه S-Q5 رو به عنوان مقصد انتخاب کن.
- اگه گوشی از طریق USB شناسایی نشد، می‌تونی APK رو export بگیری
  (Build → Build Bundle(s) / APK(s) → Build APK(s)) و با همون روش ADB
  که قبلاً برات کار کرده (adb install) نصبش کنی.

### ۴. تست
- توی اپ روی "Test HTTPS Connection" بزن.
- نتیجه رو همینجا برام بفرست (متن SUCCESS یا FAILED با جزئیات خطا).

## اگه نتیجه FAILED بود
یعنی TLS حتی با Conscrypt هم روی این هاردور/رام مشکل داره (نادره ولی ممکنه).
اونوقت می‌ریم سراغ راه‌حل HTTP خام:
- استفاده از ngrok (آدرس http:// بدون هیچ TLS)
- یا Cloudflare با دامنه‌ی شخصی و حالت SSL روی Off

## اگه نتیجه SUCCESS بود
عالیه! یعنی می‌تونیم مستقیم بریم سراغ ساخت اپ اصلی دستیار صوتی، با همین
پایه‌ی شبکه (OkHttp + Conscrypt) که الان تست کردیم.
