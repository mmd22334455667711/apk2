# ساخت APK بدون نصب هیچ‌چیز روی سیستم (با GitHub Actions)

با این روش نیازی به Android Studio، SDK، یا هیچ نصبی روی سیستم خودت نداری.
همه‌چیز روی سرورهای گیت‌هاب (رایگان) انجام می‌شه.

## مرحله ۱: ساخت اکانت گیت‌هاب (اگه نداری)
برو به https://github.com و یک اکانت رایگان بساز.

## مرحله ۲: ساخت یک Repository جدید
1. روی دکمه‌ی سبز "New" یا "+" بالای صفحه بزن → "New repository"
2. اسمش رو بذار مثلاً `tls-test-app`
3. می‌تونی Private یا Public بذاری (فرقی نداره، هر دو مجانیه)
4. دکمه‌ی "Create repository" رو بزن

## مرحله ۳: آپلود فایل‌های پروژه (بدون نیاز به دستور git)
1. توی صفحه‌ی repository تازه‌ساخته‌شده، دنبال لینک
   "uploading an existing file" بگرد (یا "Add file" → "Upload files")
2. کل محتوای پوشه‌ی TlsTestApp (همه‌ی فایل‌ها و پوشه‌ها، از جمله پوشه‌ی
   مخفی `.github`) رو بکش و بنداز توی اون کادر آپلود
   - نکته: پوشه‌ی `.github` ممکنه توی فایل منیجرت مخفی باشه؛ نمایش
     فایل‌های مخفی رو فعال کن تا ببینیش (توی ویندوز: View → Hidden items)
3. پایین صفحه دکمه‌ی سبز "Commit changes" رو بزن

## مرحله ۴: منتظر بمون تا خودش بسازه
1. برو به تب "Actions" بالای صفحه‌ی repository
2. یه پروسه به اسم "Build APK" باید به‌صورت خودکار شروع شده باشه
   (چون به‌محض آپلود، workflow فعال می‌شه)
3. روش کلیک کن، صبر کن (معمولاً ۲ تا ۵ دقیقه طول می‌کشه) تا علامت
   سبز ✅ بخوره

## مرحله ۵: دانلود APK
1. همون صفحه‌ی run که تمام شده رو باز کن
2. پایین صفحه، بخش "Artifacts" رو پیدا کن
3. روی "app-debug-apk" کلیک کن تا دانلودش کنی (یه فایل zip میاد که
   داخلش APK هست)

## مرحله ۶: نصب روی گوشی
همون روشی که قبلاً برات کار کرده رو استفاده کن:
```
adb install app-debug.apk
```

## اگه Build با خطا مواجه شد (❌ قرمز)
1. روی run بزن، روی step ای که خطا داده کلیک کن تا لاگ کامل رو ببینی
2. متن خطا رو کپی کن و برام بفرست، باهم حلش می‌کنیم
   (معمولاً مشکلات رایج مربوط به نسخه‌ی Gradle یا AGP هست که راحت
   قابل رفعه)

## بعداً: هر بار که کد رو تغییر دادی
کافیه فایل جدید رو دوباره آپلود کنی و Commit بزنی (یا فایل رو edit کنی
مستقیم توی گیت‌هاب) — به‌محض هر Commit، ساخت APK جدید خودش شروع می‌شه.
